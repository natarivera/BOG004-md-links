{
  "name": "md-links",
  "version": "0.1.0",
  "engines": {
    "node": ">=14.x"
  },
  "devDependencies": {
    "jest": "^27.5.1"
  },
  "scripts": {
    "test": "jest --coverage",
    "eslint": "eslint --ext .js ./ test/",
    "pretest": "npm run eslint"
  },
  "bin": {
    "md-links": "./cli.js"
  },
  "dependencies": {
    "eslint": "^8.15.0"
  }
}
